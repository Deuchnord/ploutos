<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.deuchnord.gestioncarteru.MainActivity" >

    <TextView
            android:layout_width="544dp"
            android:layout_height="wrap_content"
            android:id="@+id/remainingMoney"
            android:textSize="25sp"
            android:gravity="center"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <ListView
        android:id="@+id/listTransactions"
        android:layout_width="618dp"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:layout_below="@+id/remainingMoney"
        android:layout_alignRight="@+id/remainingMoney"
        android:layout_alignEnd="@+id/remainingMoney"
        android:layout_alignLeft="@+id/remainingMoney"
        android:layout_alignStart="@+id/remainingMoney">
    </ListView>

</RelativeLayout>
